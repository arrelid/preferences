# Usage: Create the symlink '~/.profile' and point it to this file
# Author: Mattias Arrelid

# Set the user mask for all newly created files
umask 022

# If there's an interactive bash shell, load .bashrc
[ -n "$BASH_VERSION" ] && [ -f ~/.bashrc ] && . ~/.bashrc

# Restrict broadcast messages to the console (see link below for details)
# http://tldp.org/howto/remote-serial-console-howto/security-messages.html
[ -x /usr/bin/mesg -a -x /usr/bin/tty -a `/usr/bin/tty` = /dev/ttyS0 ] && /usr/bin/mesg n